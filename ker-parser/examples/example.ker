# Example .ker configuration
app_name = "SuperApp"
version = 1.2
debug = true

database {
    host = "localhost"
    port = 5432
    users = ["admin", "guest", "test"]
}

features {
    logging = true
    analytics = false
    max_connections = 100
}
